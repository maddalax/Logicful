/* src/features/form/edit/FieldConfiguration.svelte generated by Svelte v3.25.1 */
import {
	SvelteComponent,
	add_render_callback,
	attr,
	create_bidirectional_transition,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal
} from "../../../../web_modules/svelte/internal.js";

function create_fragment(ctx) {
	let div1;
	let div1_transition;
	let current;

	return {
		c() {
			div1 = element("div");

			div1.innerHTML = `<h4>Field Configuration</h4> 
  <div style="padding: .85em 1em; border-radius: 1em"><label for="formGroupExampleInput2" class="form-label">Name</label> 
    <input type="text" class="form-control" id="formGroupExampleInput2" placeholder="Another input placeholder"/></div>`;

			attr(div1, "class", "mb-3");
		},
		m(target, anchor) {
			insert(target, div1, anchor);
			current = true;
		},
		p: noop,
		i(local) {
			if (current) return;

			add_render_callback(() => {
				if (!div1_transition) div1_transition = create_bidirectional_transition(div1, fade, { duration: 300 }, true);
				div1_transition.run(1);
			});

			current = true;
		},
		o(local) {
			if (!div1_transition) div1_transition = create_bidirectional_transition(div1, fade, { duration: 300 }, false);
			div1_transition.run(0);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(div1);
			if (detaching && div1_transition) div1_transition.end();
		}
	};
}

class FieldConfiguration extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, null, create_fragment, safe_not_equal, {});
	}
}

export default FieldConfiguration;