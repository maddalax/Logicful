/* src/App.svelte generated by Svelte v3.25.1 */
import {
	SvelteComponent,
	append,
	attr,
	create_component,
	destroy_component,
	detach,
	element,
	init,
	insert,
	mount_component,
	noop,
	safe_not_equal,
	space,
	transition_in,
	transition_out
} from "../web_modules/svelte/internal.js";

import { afterUpdate, onMount } from "../web_modules/svelte.js";
import AppRouter from "./AppRouter.js";
import ErrorDialog from "./components/ErrorDialog.js";
import { me } from "./services/AuthService.js";

function create_fragment(ctx) {
	let div;
	let errordialog;
	let t;
	let approuter;
	let current;
	errordialog = new ErrorDialog({});
	approuter = new AppRouter({});

	return {
		c() {
			div = element("div");
			create_component(errordialog.$$.fragment);
			t = space();
			create_component(approuter.$$.fragment);
			attr(div, "class", "App");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			mount_component(errordialog, div, null);
			append(div, t);
			mount_component(approuter, div, null);
			current = true;
		},
		p: noop,
		i(local) {
			if (current) return;
			transition_in(errordialog.$$.fragment, local);
			transition_in(approuter.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(errordialog.$$.fragment, local);
			transition_out(approuter.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(div);
			destroy_component(errordialog);
			destroy_component(approuter);
		}
	};
}

function instance($$self) {
	let setHeap = false;

	function initHeap() {
		if (setHeap) {
			return;
		}

		const user = me();

		if (user) {
			console.log(user);

			//@ts-ignore
			console.log(window.heap);

			console.log("Setting heap");

			//@ts-ignore
			window.heap.identify(user.id);

			//@ts-ignore
			window.heap.addUserProperties({ "email": user.email });

			setHeap = true;
		}
	}

	afterUpdate(() => {
		initHeap();
	});

	onMount(() => {
		initHeap();
	});

	return [];
}

class App extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default App;