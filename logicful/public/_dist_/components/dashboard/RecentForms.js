import './RecentForms.css.proxy.js';
/* src/components/dashboard/RecentForms.svelte generated by Svelte v3.25.1 */
import {
	SvelteComponent,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	set_style
} from "../../../web_modules/svelte/internal.js";

import FormList from "../../features/folders/FormList.js";
import { onMount } from "../../../web_modules/svelte.js";

function create_fragment(ctx) {
	let div;

	return {
		c() {
			div = element("div");
			div.innerHTML = `<h5 class="h5 svelte-9qol1y">Recently Viewed Forms</h5>`;
			set_style(div, "width", "100%");
		},
		m(target, anchor) {
			insert(target, div, anchor);
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(div);
		}
	};
}

function getRecentFroms() {
	return [];
}

function instance($$self) {
	
	let forms = [];

	onMount(() => {
		forms = getRecentFroms();
	});

	return [];
}

class RecentForms extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default RecentForms;